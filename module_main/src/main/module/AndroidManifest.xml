<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.anylife.module_main">

    <application
        android:name="debug.MainModuleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme"
        tools:replace="android:label">


        <activity
            android:name=".ModuleMainLauncherActivity"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".business.login.LoginActivity"
            android:launchMode="singleTop"
            android:theme="@style/FullscreenTheme" />

        <activity
            android:name=".business.navigation.MainActivityBottomNavi"
            android:launchMode="singleTask" />


    </application>

</manifest>