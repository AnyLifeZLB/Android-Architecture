<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anylife.module_ai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">

        <activity android:name="debug.AILauncherActivity"/>

        <activity
            android:name="com.arcsoft.arcfacedemo.activity.PreviewActivity"
            android:launchMode="singleTop" />

        <activity
            android:name="com.arcsoft.arcfacedemo.activity.SingleImageActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTop" />

        <activity
            android:name="com.arcsoft.arcfacedemo.activity.MultiImageActivity"
            android:launchMode="singleTop" />

        <activity
            android:name="com.arcsoft.arcfacedemo.activity.RegisterAndRecognizeActivity"
            android:theme="@style/FullscreenTheme"
            android:launchMode="singleTop" />

        <activity
            android:name="com.arcsoft.arcfacedemo.activity.FaceManageActivity"
            android:launchMode="singleTop" />

        <!--<provider-->
        <!--android:name="android.support.v4.content.FileProvider"-->
        <!--android:authorities="${applicationId}.provider"-->
        <!--android:exported="false"-->
        <!--android:grantUriPermissions="true">-->
        <!--<meta-data-->
        <!--android:name="android.support.FILE_PROVIDER_PATHS"-->
        <!--android:resource="@xml/provider_paths" />-->
        <!--</provider>-->



    </application>

</manifest>